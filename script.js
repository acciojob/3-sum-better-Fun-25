function threeSum(arr, target) {
// write your code here
	let ind,sum=0;
  arr.sort((a,b)=>a-b;) ;
	arr.map((index)=> if(arr[i]==target) ind=index;);
	sum=arr[ind-1]+arr[ind]+arr[ind+1];
	
}

module.exports = threeSum;
